export const Statuses: { [key in string]: 'idle' | 'loading' | 'failed' } = {
  Idle: 'idle',
  Loading: 'loading',
  Failed: 'failed',
};
export const ProductConditions = {
  New: 'new',
  Used: 'used',
};
